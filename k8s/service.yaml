apiVersion: v1
kind: Service
metadata:
  name: stockbroker
  namespace: stockbroker
spec:
  type: NodePort
  selector:
    app: stockbroker
  ports:
    - name: http
      port: 80
      targetPort: 8000